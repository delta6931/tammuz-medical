import { buildMetadata, GraceySelectorPage } from "../../../tools/gracey-selector/shared";

export const metadata = buildMetadata("TR");

export default function Page() {
  return <GraceySelectorPage locale="TR" />;
}
