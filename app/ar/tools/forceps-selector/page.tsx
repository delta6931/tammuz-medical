import { buildMetadata, ForcepsSelectorPage } from "../../../tools/forceps-selector/shared";

export const metadata = buildMetadata("AR");

export default function Page() {
  return <ForcepsSelectorPage locale="AR" />;
}
